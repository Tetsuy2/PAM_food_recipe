import 'package:flutter/material.dart';

const green = Color(0xFF2DA67A);
const chipGrey = Color(0xFFF1F4F6);
const card = Colors.white;
const line = Color(0xFFE9EDF0);

final appScheme = ColorScheme.fromSeed(
  seedColor: green,
  primary: green,
  surface: card,
  outline: line,
);
